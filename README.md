# Walk'n Paw

Walk'n Paw is a small, static marketing site for a dog walking service in Magdeburg. It is built with React, Vite, and TypeScript, and most copy lives in `src/content` JSON files so it can be managed via a CMS workflow.

Build: `npm run build`
